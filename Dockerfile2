FROM python:3.7

WORKDIR /api

COPY requirements.txt ./requirements.txt

COPY models/ /api/models/

COPY api/ /api/api/

RUN ls -R /api

# EXPOSE 8080

RUN pip install -r requirements.txt

CMD python run /api/api/app.py --server.port $PORT

